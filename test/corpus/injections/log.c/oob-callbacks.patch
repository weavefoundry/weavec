--- a/src/log.c
+++ b/src/log.c
@@ -154,7 +154,7 @@
     va_end(ev.ap);
   }
 
-  for (int i = 0; i < MAX_CALLBACKS && L.callbacks[i].fn; i++) {
+  for (int i = 0; i <= MAX_CALLBACKS && L.callbacks[i].fn; i++) { /* INJECTED: reads callbacks[MAX_CALLBACKS] */
     Callback *cb = &L.callbacks[i];
     if (level >= cb->level) {
       init_event(&ev, cb->udata);
