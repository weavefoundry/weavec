--- a/sds.c
+++ b/sds.c
@@ -165,6 +165,7 @@
 void sdsfree(sds s) {
     if (s == NULL) return;
     s_free((char*)s-sdsHdrSize(s[-1]));
+    s[0] = 0; /* INJECTED: use after free */
 }
 
 /* Set the sds string length to the length as obtained with strlen(), so
