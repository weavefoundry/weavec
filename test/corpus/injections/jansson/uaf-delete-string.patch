--- a/src/value.c
+++ b/src/value.c
@@ -819,6 +819,7 @@
 static void json_delete_string(json_string_t *string) {
     jsonp_free(string->value);
     jsonp_free(string);
+    string->length = 0; /* INJECTED: use after free */
 }
 
 static int json_string_equal(const json_t *string1, const json_t *string2) {
