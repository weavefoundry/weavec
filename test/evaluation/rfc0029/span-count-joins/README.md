# Joined byte-span counts (RFC 0029)

Frozen before candidate 50. These cases retain a returned count established
on separate branches, including an assignment before its validating guard.
The false count and post-guard mutation must remain rejected.
