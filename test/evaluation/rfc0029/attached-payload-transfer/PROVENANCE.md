RFC 0029 attached-payload transfer population, frozen against candidate 102
while adding the fresh-region container-combined output, its forwarding
through a direct return, and its installation on an immediate result test.
A complete attach publishes the disjoint union of both owned inputs and the
key it allocates itself on success, and preserves both inputs separately on
allocation failure. The client tests the forwarded result. Losing the item
on the failure path, releasing it twice after a successful transfer, and
ignoring the result all remain required rejections.
