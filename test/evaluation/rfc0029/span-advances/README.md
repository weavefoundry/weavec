# Returned span counts used to advance cursors (RFC 0029)

Frozen before candidate 57. Capture and use the actual count and entry
coordinates. A changed step or a count larger than the span remains rejected.
