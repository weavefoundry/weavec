The original byte-switch-partitions population is retained: helper and negative scanner filenames collided with client filenames, causing invocation failures. This independently frozen population gives those helper files distinct names while preserving every intended case and expectation. Baseline candidate88c predates byte-switch for-loop partitioning. The unchanged 32-partition bound and later-backedge fallback remain required.
