Frozen before the fixed-step span proof correction. A complete generic helper
returns zero or exactly two bytes, bounded by its input span. Actual callers
advance within initialized arrays of even, odd, and runtime lengths. Invalid
variants overstate or change the step, pass unrelated endpoints, or leave input
uninitialized. Candidate72c is the unchanged baseline.
