The initial frozen inventory is retained, with candidate26 baseline results in cursor-reader27-baseline.json. Before reader-predicate code changes, inspection found that the generic helper is allowed sufficient entry premises, so applying a zero-entry-premise expectation to both it and main was invalid; the runner does not recognize entry_functions. In addition, the initial uninitialized/capacity callers read only valid initialized data[0], and the off-by-one helper was called below its faulty endpoint. A bounded-reader output alone is not required of every ordinary helper. The reviewed manifest therefore tests generic conditional completeness separately from zero-premise closed clients and uses concrete erroneous accesses for the negatives. It adds reads after cursor escape and at the invalid capacity/initialization endpoints. No original file or expectation was edited. This audit precedes reader implementation.
