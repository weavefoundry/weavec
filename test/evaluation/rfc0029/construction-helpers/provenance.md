Independent tree-construction probes frozen before admitting verified external helpers in recursive contract candidates. Successful construction returns a fresh initialized binary forest; failure of the second recursive allocation destroys the already-built left subtree. The first-byte helper is read-only and inferred. Negative expectations cover missing partial-tree cleanup, duplicate cleanup, and a short input interval. No annotations or third-party summaries are used.
