/* RFC 0028: independent regression beyond the frozen population. */
#include "../library.h"
void a_configure(unsigned); void b_configure(unsigned);
void a_destroy(struct node *); void b_destroy(struct node *);
